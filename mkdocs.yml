site_name: arxiv2026_phantom_specialization
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: arxiv2026_phantom_specialization
      handlers:
        python:
          options:
            docstring_style: numpy